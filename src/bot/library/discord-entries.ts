/**
 * 本机 Discord 适配词条 —— 词库的**最低优先级**补充层。
 *
 * Dice! 的内置帮助（`GlobalVar.cpp` 的 `HelpDoc`）只有 QQ 端命令；本端口的
 * `/game`（对局/子区）、`/log`（跑团日志）、`/rh`（暗骰子区）等命令是
 * `docs/Discord_CoC_Command_Set.md` 的本地设计，上游离线数据里没有对应词条。
 *
 * 因此这里保留一份**很小**的补充表，供 `/help query:<本机词条>` 与
 * 「你是不是想找」使用；同名时 Dice! 内置词条与外部词库优先（见 `library.ts` 的合并顺序）。
 * COC7 房规/成功等级等规则术语同理：Dice! 的规则书词条是运行时从 `api.kokona.tech`
 * 下载的（`GetRule.cpp`），离线不可得，这里给出规格（docs §8.1）转写的本机口径。
 */

/** `/help` 用的本机词条。 */
export const DISCORD_HELP_ENTRIES: Record<string, string> = {
  本机扩展:
    '本机在 Dice! 词库之外提供的 Discord 指令（详见 docs/Discord_CoC_Command_Set.md）：\n' +
    '`/r` `/rs` 掷骰、`/rh` 暗骰；`/pc` 角色卡、`/st` 属性；`/rc` `/ra` 检定、`/sc` 理智、`/en` 成长、`/setcoc` 房规、`/ti` `/li` 疯狂症状；\n' +
    '`/game` 对局（start/list/switch/state/end）、`/log` 跑团日志（new/list/on/off/end）、`/rules` 规则速查；\n' +
    '`/nn` `/nnn` 称呼与随机称呼、`/name` 随机姓名、`/help` 本词库。',
  开局:
    '`/game start name:阿卡姆 keeper:@KP`：默认在当前频道新建公开子区作主场景，并在父频道下建私密子区 `暗骰 · <桌名>`、自动开启日志、把当前场景切到本局。`thread:` 复用已有子区，`here:true` 就地开局。',
  暗骰:
    '`/rh`：结果投递到私密子区。优先级 = 本次 `thread` > 本局暗骰子区 > 场景级登记 > 自动 `暗骰 · <场景名>`（不同子区各自一个）。`/rh thread:<子区>` 会登记并可省略，`/rh reset:true` 取消登记（只清场景级）。**有局时只把本局 KP 拉进子区**（KP 由 `/game start keeper:` 指定），其他人只收到仅自己可见的回执；缺少权限时降级为仅你可见。',
  日志:
    '`/log new` 开新日志（当前日志仍在记录时会被拒绝，先 `/log end` 或 `/log off`）；`/log list` 查看；`/log end [name:]` 结束并导出附件。暂停中的旧日志不会被自动结束。',
  房规: '`/setcoc set rule:0-6`、`/setcoc show`、`/setcoc clr`。有局时设本局房规（局内所有场景共享），无局时设场景房规；生效顺序：本局 → 场景 → 骰主默认。',
  检定: '`/rc 力量`、`/rc 困难智力 99`、`/rc 3#p 手枪`：整行原文解析，成功率、骰值、成功等级与所用房规一并回执。',
  暗骰子区:
    '本局私密子区，`/game start` 时自动创建（建在主场景父频道下，子区不能内嵌子区），`/rh` 默认投递到这里。',
  角色卡:
    '`/pc new name:卡特` 新建，`/pc tag name:卡特` 绑定。有局时绑定到**局**（局内所有场景共享、切局自动跟随），无局时绑定到场景；读取顺序：本局 → 场景 → 全局默认卡。',
  切换局:
    '`/game switch game:#1`：把当前场景切到指定局，角色卡 / 暗骰子区 / 房规 / 日志随之切换。限服务器管理员或该局 KP。',
  结束局:
    '`/game end [game:] [archive:true]`：导出本局所有未结束日志、解绑其场景。默认**不归档**任何子区，只有 `archive:true` 才归档 Bot 自建的主场景子区与暗骰子区。',
};

/** `/rules query:<词条>` 用的本机规则术语（规格 §8.1 转写，与 `/setcoc` 0-6 档一致）。 */
export const DISCORD_RULE_ENTRIES: Record<string, string> = {
  大失败:
    '房规决定（/setcoc）：0 规则书 = c 不满 50 出 96-100、满 50 出 100；2 = 出 100 或 96-99 且高于成功率；3 = 96-100；6 = 个位数 = 十位数且高于成功率。',
  大成功:
    '房规决定（/setcoc）：0 = 出 1；1 = 不满 50 出 1、满 50 出 1-5；2/3 = 出 1-5（2 需 ≤ 成功率）；4 = 出 1-5 且 ≤ 成功率/10；5 = 出 1-2 且 < 成功率/5；6 = 个位数 = 十位数且 ≤ 成功率。',
  成功等级: '大成功 > 极难成功（≤ 成功率/5）> 困难成功（≤ 成功率/2）> 成功（≤ 成功率）> 失败 > 大失败。',
  困难成功: '骰值 ≤ 成功率 ÷ 2。',
  极难成功: '骰值 ≤ 成功率 ÷ 5。',
  孤注一掷: '失败后可以孤注一掷再试一次，但必须给出新的做法；再次失败会有更严重的后果（KP 裁定）。',
  成长: '`/en 技能名 [技能值]`：投 1d100，大于当前技能值则成长 1d10（或按房规），并把新值写回角色卡。',
  理智: '`/sc 成功损失/失败损失 [当前san]`：失败按失败损失扣，大失败扣最大损失；san 会自动回写角色卡。',
  临时疯狂: '`/ti`：单次失去 ≥5 点理智后触发临时疯狂，投 1d10 查症状表。',
  跳跃: '跳跃（40%）：跳过 3 米宽的缺口或 1 米高的障碍；更远更难时由 KP 调整难度。',
  侦查: '侦查：发现隐藏线索、暗门或被搜身者身上的物品，由 KP 决定是否给予加成。',
};
